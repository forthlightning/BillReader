<%= flash[:notice] %>
OI VATO! This is where all the users, and their info will be listed.
  
  <tbody>

        <table>
      <thead>
        <tr>
          <th>Id</th>
          <th>Name</th>
          <th>Survey</th>
        </tr>
      </thead>
    <% if @users.nil? %>
    <% puts "No users yet, Upload something!" %>
    <% else @users.each do |user| %>


      <tr>
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td><%= user.survey %></td>
        <td><%= link_to "Show...", user_path(user), data: {no_turbolink: true} %>

        	

      </tr>
    <% end %>
    <% end %>


  </tbody>
</table>

<h3>Let's add a new user... or something!</h3>

<%= link_to 'Add User', new_user_path %>

